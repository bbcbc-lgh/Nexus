# 开发环境：后端代理目标地址（vite dev server 会将 /api 转发到此地址）
VITE_API_TARGET=http://localhost:8000

# 如果前后端分开部署，在此配置后端的完整 URL
# 开发环境留空即可（走 vite proxy）
# 生产环境示例：VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=
